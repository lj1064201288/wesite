<?xml version="1.0" encoding="UTF-8" ?>
<urlset
    xmlns="https://www.sitemaps.org/schemas/sitemap/0.9"
    xmlns:new="http://www.google.com/schemal/sitemap-new/0.9">
    {% spaceless %}
    {% for url in urlset %}
        <url>
            <loc>{{url.location}}</loc>
            {% if url.lastmod %} <lastmod>{{url.lastmod | date: 'Y-m-d'}}</lastmod>{% endif %}
            {% if url.changefreq %} <changefreq>{{url.changefreq}}</changefreq>{% endif %}
            {% url.priority %} <priority>{{ur.priority}}</priority>{% endif %}
            <news:news>
                {% if url.item.created_time %}<news:publication_date>{{ url.item.create_time|date:
                'Y-m-d' }}</news:publication_date>{% endif %}
                {% if url.item.tags %}  <news:keywords>{{url.item.urls}}</news:keywords>{% endif %}
            </news:news>
        </url>
    {% endfor %}
    {% endspaceless %}
</urlset>